<h1>Home Page</h1>

<table>
    <tr>
        <td>Type</td>
        <td>Value</td>
    </tr>

    @foreach (var claim in ViewData["UserClaims"] as List<System.Security.Claims.Claim>)
    {
        <tr>
            <td>@claim.Type</td>
            <td>@claim.Value</td>
        </tr>
    }
</table>